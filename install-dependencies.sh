apt update
apt install net-tools smartmontools sysstat

#apt install python-pip
#pip install pymongo

echo ''
echo View notes in nginx.conf, fpm.conf, mysql.conf and smartctl.conf.
echo Smartctl is only needed on physical servers.
